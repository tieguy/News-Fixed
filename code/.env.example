# Anthropic API Key for Claude AI content generation
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Print command for newspaper printing (default: lpr)
# The PDF path will be appended to this command
# Examples:
#   PRINT_COMMAND=lpr -P Brother_HL
#   PRINT_COMMAND=lp -d myprinter -o fit-to-page
#   PRINT_COMMAND=lpr -o media=letter
PRINT_COMMAND=lpr

# News mode: controls what content is included
#   'family' - personalized content (Duke sports, SF local news, xkcd comics)
#   'friends' - generic content only (second main story instead of personalized)
# Default: 'family' for local CLI, 'friends' for web deployment
NEWS_MODE=family
