# SPDX-FileCopyrightText: 2025 Luis Villa <luis@lu.is>
#
# SPDX-License-Identifier: BlueOak-1.0.0

# fly.io configuration for News Fixed web application
# See: https://fly.io/docs/reference/configuration/

app = 'news-fixed'
primary_region = 'sjc'

[build]
  dockerfile = "Dockerfile"

[processes]
  # Single process runs both web server and cron scheduler via start.sh
  # Machine stays running 24/7 so supercronic can fire on schedule (see crontab)
  app = "/app/start.sh"

[env]
  CACHE_DIR = "/app/cache"
  PYTHONUNBUFFERED = "1"
  # NEWS_MODE: 'family' (personalized: sports, local news, xkcd) or 'friends' (generic content)
  NEWS_MODE = "friends"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[http_service.checks]]
  grace_period = "30s"
  interval = "30s"
  method = "GET"
  path = "/health"
  timeout = "5s"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1

[[mounts]]
  source = "news_fixed_cache"
  destination = "/app/cache"
