# Anthropic API Key
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Print command for newspaper printing (default: lpr with duplex)
# The PDF path will be appended to this command
# Default uses double-sided (duplex) printing for the 2-page newspaper
# Examples:
#   PRINT_COMMAND=lpr -P Brother_HL -o sides=two-sided-long-edge
#   PRINT_COMMAND=lp -d myprinter -o sides=two-sided-long-edge
#   PRINT_COMMAND=lpr -o media=letter -o sides=two-sided-long-edge
# To disable duplex (single-sided):
#   PRINT_COMMAND=lpr -o sides=one-sided
PRINT_COMMAND=lpr -o sides=two-sided-long-edge

# News mode: controls what content is included
#   'family' - personalized content (Duke sports, SF local news, xkcd comics)
#   'friends' - generic content only (second main story instead of personalized)
# Default: 'family' for local CLI, 'friends' for web deployment
NEWS_MODE=family
